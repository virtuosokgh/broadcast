# TV 편성표 웹사이트

실제 방송 편성표를 확인할 수 있는 웹 애플리케이션입니다.

## 🚀 시작하기

### 설치

```bash
npm install
```

### 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:5173`으로 접속하세요.

## 📡 실제 편성표 사용하기

### 1. TVmaze API (기본값, 무료)

TVmaze API는 CORS를 지원하므로 별도 설정 없이 바로 사용할 수 있습니다. 하지만 한국 채널 데이터는 제한적일 수 있습니다.

**사용 방법:**
- 상단의 "실제 편성표 사용" 체크박스를 활성화
- API 소스에서 "TVmaze API" 선택

### 2. 공공데이터포털 API

한국 정부에서 제공하는 공공데이터포털의 방송편성표 API를 사용할 수 있습니다.

**설정 방법:**
1. [공공데이터포털](https://www.data.go.kr)에 회원가입
2. "방송편성표" 또는 관련 API 검색
3. API 키 발급
4. 프로젝트 루트에 `.env` 파일 생성:
   ```
   VITE_DATA_GO_KR_API_KEY=발급받은_API_키
   ```
5. 개발 서버 재시작

**사용 방법:**
- 상단의 "실제 편성표 사용" 체크박스를 활성화
- API 소스에서 "공공데이터포털" 선택

### 3. KBS API

KBS에서 제공하는 편성표 데이터를 사용할 수 있습니다.

**주의사항:**
- CORS 정책으로 인해 브라우저에서 직접 호출이 제한될 수 있습니다
- 프록시 서버가 필요할 수 있습니다

**사용 방법:**
- 상단의 "실제 편성표 사용" 체크박스를 활성화
- API 소스에서 "KBS API" 선택

## 🔧 API 엔드포인트 수정

실제 API 엔드포인트가 다른 경우, `src/services/tvScheduleAPI.js` 파일을 수정하세요:

```javascript
// 공공데이터포털 API 엔드포인트 수정
const baseURL = '실제_API_엔드포인트';

// KBS API 엔드포인트 수정
const url = `실제_KBS_API_URL`;
```

## 📝 참고사항

- **CORS 문제**: 일부 API는 브라우저에서 직접 호출이 제한될 수 있습니다. 이 경우 프록시 서버가 필요합니다.
- **API 키 보안**: `.env` 파일은 Git에 커밋하지 마세요. `.gitignore`에 추가되어 있습니다.
- **데이터 형식**: API 응답 형식이 다를 경우 `parseScheduleData`, `parseKBSSchedule`, `parseTVmazeSchedule` 함수를 수정하세요.

## 🛠 기술 스택

- React
- Vite
- CSS3

## 📄 라이선스

이 프로젝트는 개인 사용 목적으로 제작되었습니다. 실제 방송 편성표 데이터 사용 시 각 방송사 및 API 제공사의 이용약관을 확인하세요.
